<%= form_for [:admin, ingredient], html: { multipart: true } do |f| %>
  <%= f.label :name %> <%= f.text_field :name %><br />
  <%= f.label :group_id %> <%= f.select :group_id, Group.order(:name).all.map {|g| [g.name, g.id] }, include_blank: true %><br />
  <%= f.submit %>
<% end %>
